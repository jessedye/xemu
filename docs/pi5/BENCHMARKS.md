# Pi 5 benchmark ledger

Every performance claim on the Pi 5 branch traces back to a row here. The
rules that keep the numbers honest:

- **One variable at a time.** A/B pairs run on the same binary whenever the
  change is runtime-gated (env var / config), otherwise consecutive builds
  with only the change under test between them.
- **Bounded, repeatable runs.** `contrib/pi5/bench.sh` (175 s, Halo 2, 1080p
  output, `timeout -s KILL`), analysed with `scripts/perflog_report.py`
  (`summary` for one run, `compare` for a pair — its noise verdict decides
  whether a delta is real).
- **Fairness check.** Compare the peak `xtrace: surface_update count=` between
  the runs. A run whose guest did 1000x less drawing was starved; its fps is
  meaningless however plausible it looks. `perflog_report.py compare` flags
  this automatically from the sibling `.log`.
- **Record the binary.** `xemu_commit:` from the run log identifies what
  actually ran; a benchmark of a stale binary has happened once already.

## Environment

Raspberry Pi 5, 2 GB, Cortex-A76 4x2.8 GHz, V3D 7.1 @ 1350 MHz (VideoCore
VII), V3DV Mesa Vulkan 1.2, CMA 512 MB, Debian 12. Benchmark title: Halo 2
(caps at 30 fps — the "native" target for this table).

## Ledger

| # | Change | Before | After | Verdict |
|---|--------|--------|-------|---------|
| 103 | **Alias-only-when-free validated: Halo 2 +31.7% with every other title untouched** | first attempt (`d2380605c9`) tested whether a barrier was *needed* rather than whether it would *cost a boundary*. It fired 84/frame in Halo 2, **24 in Halo 1's cryo bay which pays none**, and 10.7 in GTA where only 4 cost anything - **GTA -5.9% fps** and a p99 regression, nearly the whole penalty of dropping aliasing outright. The tell was two counters disagreeing: old `rpb_nd_alias` read 0.00 for the cryo bay while the new `alias_downgrade` read 24.00 | adding `r->in_render_pass` (`01d8c8e22f`) makes them agree **exactly**: downgrades are now 52.00 / 0.00 / 3.98 / 0.68 / 0.00 against original counts of 52.00 / 0.00 / 3.99 / 0.68 / 0.00. Results vs shipping: **halo2 8.90 -> 11.72 fps (+31.7%), 1% low +54.0%, p99 -35.9%, `gpu_busy` -42.0%, `renderpasses` 75 -> 26**; gtaplay 26.76 -> 26.92; halo_cryo 19.55 -> 19.43; thps 45.08 -> 45.02; mwplay 13.54 -> 13.51 | **shipped.** Two `stutters` flags resolved rather than argued away: `stutters` is a count per window, so Halo 2's +36.7% is 0.959 -> 0.996 **per frame** once normalised by the 45 -> 59 frames a faster build renders; GTA's runs were [32, 7] against a [12, 12] baseline, a 4.5x spread inside one configuration. Only 3 copies a frame result from 52 downgrades - the texture cache absorbs the rest, since `copy_surface_to_texture` re-runs only when `draw_time` changes |
| 101 | **Halo 2 finally has a gameplay snapshot, and direct surface sampling costs it a third of its frame rate** | Halo 2 is the title the owner names most and the only one never benchmarked from a snapshot - its old ones abort on restore in `gp_ep.c: scatter_gather_rw`. Drove it from a wiped profile (title `4d530064`) into Cairo Station's armory and saved `halo2_play`: **9.28 fps, 75 render passes/frame, `gpu_busy` 44.67 ms, `wait:surface_down` 53.99 ms**. The new non-draw counters attribute it at once: **`rpb_nd_alias` 52.00/frame - 69% of all boundaries, 20.8 ms** - with `rpb_nd_copy` **0.00** | `XEMU_SURF_TEX_SAMPLE=0`, two runs per arm: **fps 8.90 -> 11.89 (+33.6%)**, p50 -26.6%, p99 -39.7%, **1% low 5.07 -> 8.30 (+63.6%)**, `gpu_busy` -43.0%, **`renderpasses` 75 -> 23**, rendering pixel-equivalent in a side-by-side capture. **The boundaries do not come back** - the drop is exactly the 52 alias barriers, with `surf_bind` and `clear` unchanged - the first change all session where pass breaks were not conserved, because aliasing *adds* barriers rather than relocating them | the flag has been on globally since row 30 on the strength of stutters -15% and 1% low +7%. Three prerequisites made this findable and two were things I had doubted: the gameplay snapshot, the `rpb_nd_copy`/`rpb_nd_alias` split I called a commit I would have saved, and query-inline having already landed - 237 query breaks would have masked 52 alias ones |
| 102 | **Aliasing's cost tracks `rpb_nd_alias` exactly, so the fix is per-case, not a global flag** | flipping `XEMU_SURF_TEX_SAMPLE` off globally would be the easy win. Measured it on every snapshot instead, two runs per arm | **halo2 `rpb_nd_alias` 52.00 -> sts=0 +33.6% fps; gtaplay 3.99 -> sts=0 -5.4% fps, `gpu_busy` +7.6%, stutters +102%; thps 0.68 -> sts=0 -2.2%, `gpu_busy` +8.2%; halo_cryo 0.00 -> +1.9% (noise); mwplay 0.00, `surf_to_tex` 0.00 both ways -> inert.** Where the barrier is rare, aliasing wins by saving more copies than it costs boundaries; only at 52 a frame does it become catastrophic | **a global flip would buy Halo 2 a third of its frame rate and take 5% from GTA and 2% from THPS.** Fixed per-case in `d2380605c9`: alias only where no barrier is needed, fall back to copying where one is. The copy path's transitions already are the write-to-sample barrier, and both the initial copy at node creation and the `draw_time` refresh already exist, so the fallback is correct by construction - verified in the source before committing, not after |
| 100 | **Cryo-bay win reproduces on a third independent measurement, and holds across the suite** | after the LRU walk fix, the whole snapshot suite re-run QI off vs on. Prior cryo measurements were 19.75 fps (controlled A/B) and 19.93 (pass-attribution run) | **halo_cryo 5.27 -> 20.08 fps (+281%)**, p50 188.16 -> 50.03 (-73.4%), p99 217.71 -> 77.52 (**-64.4%**), `gpu_busy` 121.65 -> 25.97 (-78.6%), `renderpasses` 265.07 -> 28.07 (-89.4%), queries 237.0 in both arms. Elsewhere: halo outdoor 29.70 -> 29.73 with `renderpasses` -22.3%; gtaplay 25.51 -> 26.16 with p99 -8.9%; thps 43.84 -> 43.46 | **Three independent measurements now agree within 2%** (19.75 / 19.93 / 20.08) and p99 improves alongside p50, so there is no tail trade. The one flag - thps p50 +11.1% - has `queries` 0.0, `gpu_busy` +1.6% and `renderpasses` +1.7%, i.e. no mechanism changed, and its p99 moved the opposite way (-12.7%): the GTA signature. **Resolved by three runs per arm: p50 effect +0.6% against a 7.0% spread** (21.68/21.92/23.25 vs 22.41/22.24/22.61) - the one-shot +11.1% was drift, exactly as with GTA. Cross-checking pre- against post-LRU at QI=1 shows no title lost ground (cryo 19.93 -> 20.08, thps 43.06 -> 43.46, halo 29.72 -> 29.73, gta 27.13 -> 26.16 inside a 13% spread). **No regression in any title.** Third time a single-run fps or tail flag has evaporated under repeats |
| 99 | **`lru_visit_active` scanned 65536 bin heads to find 1024 nodes - fixed, but it sits off the critical path** | whole-process profile of Morrowind gameplay put `pgraph_vk_mark_textures_possibly_dirty` at **3.29%**, second only to the TCG block lookup, reached via `pgraph_vk_bind_textures -> create_texture -> check_texture_possibly_dirty -> lru_visit_active` (2.95%). Cause: `LRU_NUM_BINS` is `1<<16`, so every call read **65536 list heads spanning a megabyte** of cache-cold memory to find live nodes in a cache holding **1024**, and it runs per dirty texture bind against 1172 draw batches a frame | rewrote it to walk the `global` list (which holds every node) testing `lru_is_node_in_use`, visiting the identical set in <=1024 iterations. Verified `QTAILQ_REMOVE` nulls `tql_prev` - exactly what `QTAILQ_IN_USE` tests - and that all four visitors are order-independent. Morrowind, 3 runs vs a single-run baseline: **fps 13.29 -> 13.59 (+2.2%)**, p50 70.06 -> 67.25, `gpu_busy` and `renderpasses` unchanged - the signature of a CPU-side fix. Morrowind's own spread is only **1.3%** across 6 runs, unlike GTA's 13.2% | **Correct and kept, but do not oversell it.** A per-thread profile taken afterwards shows the guest CPU thread at **100%**, the pfifo/renderer thread at **24%**, everything else <=6% - and `mark_textures_possibly_dirty` **does not appear on the critical-path profile at all.** The fix optimised a thread with 76% headroom; the +2.2% is thread coupling (the guest blocks on the FIFO) plus a single-run baseline. **Row 76's lesson repeated: the whole-process profile was consulted before the per-thread one, and it flattered work off the critical path.** Critical path is TCG: `helper_lookup_tb_ptr` **22.44%**, x87 hard helpers ~9.7%, TLB ~3.5%. Row 77 already refuted resizing the jump cache at 13 and 15 bits; beating this needs the jump-cache probe inlined into generated code |
| 98 | **No regression from query-inline in titles without queries - and single-run fps A/Bs in GTA cannot detect anything under ~13%** | a one-shot suite flagged GTA:SA at fps -7.3%, p50 +6.4%, p99 +14.2%, 1% low -14.0% while its `queries` was **0.00/frame** and `renderpasses` 16.01 -> 16.05, `gpu_busy` 16.68 -> 16.88. A flag that changes no counter cannot cost 7% of the frame rate, so the suite was repeated three times per arm | **run-to-run spread swamps it.** QI=0 fps came out 29.03 / 25.66 / 26.26 and QI=1 26.20 / 25.50 / 27.21 - a 13.2% spread across six runs against a -2.5% arm difference. p99 spread 14.9% vs +0.1% effect; 1% low spread 15.7% vs **+4.2% in query-inline's favour**. Meanwhile `gpu_busy` spread is **1.9%** and `renderpasses` **0.7%**, both stable across all six | **NOT a regression.** Halo 1 keeps its 3.86x, Morrowind gains (p50 -13.9%, gpu -5.3%), THPS3 and GTA are inert. The methodological result matters as much: a snapshot restores into a scene that keeps evolving - traffic, AI, camera - so two runs of the SAME build are not the same workload. **Counter and GPU-time metrics are trustworthy single-shot; fps and tail metrics need repeats.** Every one-shot fps claim in this ledger under about 10% for an open-world title should be read with that in mind |
| 97 | **Remaining cryo-bay pass breaks attributed, and clear-inline refuted a second time** | with query breaks gone, 27.34 render passes remain and account for 10.94 ms of the 25.23 ms `gpu_busy` (43%). Split `rpb_surface` three ways to find out where they come from: **`rpb_surf_bind` 10.94/f (4.38 ms), `rpb_nondraw` 9.67/f (3.87 ms), `rpb_fb_dirty` 0.00** - the dirty-framebuffer path never fires here. Plus `rpb_clear` 4.87 (1.95 ms) and `rpb_finish` 1.86. The three split counters sum to 20.61 = `rpb_surface`, which validates the split | `XEMU_CLEAR_INLINE=1` removes the clear boundaries exactly as designed - **`rpb_clear` 4.87 -> 0.00** - but total `renderpasses` moves only 27.34 -> 27.19. The counters say precisely where the other 4.72 went: **`rpb_surf_bind` +4.27, `rpb_nondraw` +0.33, `rpb_finish` +0.12, summing to the -0.15 observed.** The clear coincides with a surface binding change; take away the clear's boundary and the bind takes it a moment later. fps -4.5%, gpu_busy +3.0% | **Clear-inline stays OFF - refuted twice under opposite conditions** (row 90 measured it when 237 query breaks a frame dominated; this measures it when they are gone) and now with the mechanism named rather than inferred. Conservation of pass breaks is the strongest rule in this ledger. Note for the next attempt: the two live targets are `surf_bind` and `nondraw`, both intrinsic to what the guest asks for, so expect conservation there too - the CPU side is the larger opening |
| 96 | **Occlusion queries kept inside the render pass: Halo 1 cryo bay 5.11 -> 19.75 fps** | controlled A/B on snapshot `halo_cryo`, one scene, one binary, one variable. **QI off: fps 5.11, p50 191.51 ms, p99 251.56, 1% low 3.72, queries 236.1/f, renderpasses 264.4, rpb_query 236.1, gpu_busy 121.6 ms.** Matches the owner's own session (5.16 fps, 208.8 rpb_query) - this is their scene | **QI on: fps 19.75 (3.86x), p50 49.58 (-74%), p99 82.19 (-67%), 1% low 11.11 (+199%), renderpasses 28.2, rpb_query 0.0, gpu_busy 26.0 ms (-79%).** The 0.40 ms/pass cost model predicted 236.1 x 0.40 = 94.4 ms of GPU reduction; measured 95.6 ms, **within 1.3%**. No tail cost - p99 improved with p50. Two prerequisites made this measurable at all: a gameplay snapshot in a query-heavy scene (all eight prior snapshots issue <=1 query/frame) and `pgraph_vk_env_opt_in`, because `XEMU_QUERY_INLINE=0` previously still enabled the flag and the first A/B reported all three arms identical | **CONFIRMED, largest measured win in the project.** Caveat kept deliberately: the pre-fix binary renders this scene's HUD glyph correctly too, so `c13e946e7b` fixes a real begin-outside/end-inside scope violation but is **not** demonstrated to be the cause of the owner's reported missing glyphs. The performance result does not depend on that question |
| 95 | **`XEMU_QUERY_INLINE` is NOT SAFE on V3DV - it corrupts rendering** | the switch keeps occlusion queries inside the render pass, avoiding a pass break per query; Halo CE issues ~92 queries/frame and it measured **18.0 -> 25.5 fps with GPU busy 50.7 -> 14.4 ms**, the largest single win in the project | owner A/B on his own machine: with #2982 and the switch **off**, Halo 2's menu renders correctly; with the switch **on**, the menu is heavily corrupted (garbled glyph blocks) and Halo 1 loses button icons and ghosts duplicate glyphs over highlighted menu text. Turning it off fixes both | **switch removed from the launcher; stays default-off in code.** The pass breaks it eliminates are load-bearing: they force the tile store and reload that flushes stale surface content, so removing them leaves the glyph atlas sampling garbage. This is the clearest example yet that GPU work removed from a tiler is not automatically free - a 3.5x GPU-time reduction that produces wrong pixels is not a win. Also a process lesson: it was shipped on request without measurement, and three separate visual faults were misattributed to upstream picks (#2478 twice) before the owner's own on/off comparison settled it |
| 94 | **The benchmark harness was destroying the owner's save games** | saves reported as wiped every session; theories about qcow2 writeback caching and save corruption were both wrong | `.xemu-bench.toml` and the owner's `xemu.toml` both pointed at `~/.local/share/xemu/xemu/xbox_hdd.qcow2`, and every bench run resumes a snapshot. **`loadvm` reverts the disk**, so each benchmark rolled the drive back to the snapshot date (Aug 20 05:31-12:25) and discarded everything saved since | **fixed**: the bench config now uses a private copy at `/home/pi/xbox_hdd.bench.qcow2`. This also explains Halo finding a save directory it could not load - the directory survived in the snapshot, the profile that owned it did not. Never point a snapshot-resuming harness at a disk image someone plays on |
| 93 | **Owner-reported regressions from the upstream picks, isolated** | eight upstream cherry-picks shipped together; owner reported worse gameplay, Halo 1 button icons missing and Halo 2 menu corruption | Tony Hawk, same snapshot: all eight picks **fps -9%, p50 +25%**; drop #2929 -> fps -1%, p50 +13%; drop #2478 and #2982 as well -> **fps -3%, p50 +3%, both noise**. GPU busy flat throughout, so the cost was host-side, not rendering | **all three reverted.** #2929 defers the PGRAPH IRQ to a main-loop bottom half, buying a deadlock fix we have never hit with a main-loop turn per interrupt. #2478 maps `ONE_MINUS_DST_ALPHA` -> `ZERO` on X8R8G8B8/X1A7R8G8B8, which makes anything blended that way invisible - the mechanism matches the missing icons, but a single-frame capture of Halo 2's menu came back **clean**, so neither visual fault is reproduced yet and nothing is being reported upstream on it |
| 92 | **Upstream #2941 lock-free fence polling + eight upstream cherry-picks** | #2941 reports ~580k reads of `NV_PGRAPH_PATT_COLOR0` in a Project Gotham 2 segment, each taking `pg->lock` and convoying with the FIFO puller; verified against our tree that `pgraph_read` locks unconditionally and the register falls to `default:`, so the fast path is exact for us | added a `fence_poll` counter on **both** paths before believing it: Morrowind polls it **0.55 times per frame**. fps +3%, p50 +6% REGRESSED, stutters -5% - noise, as it must be when the mechanism is absent. New draw-mix columns settle a second question: **inline_elements 695.9/frame, inline_buffers 0.00, inline_arrays 0.01** | **#2941 kept (harmless, real for PGR2-class titles) but worth nothing here** - the counter said so before any frame rate could be credited to it. The draw mix prices upstream #2930 as inert for us (it optimises INLINE_ARRAY, which we use 0.01x/frame) and is the datum #2734 and #2956 both turn on. Eight picks merged (2951 x87 precision, 2158, 2929, 2720, 2982, 2478, 2987): stability pass clean on three titles - THPS3 36.5 fps, GTA 24.5, Morrowind 23.0, scene 220-880 batches, **0 errors** |
| 91 | **R4 clip-invariant `framebuffer_dirty` REFUTED at the source** | the cross-title synthesis put Call of Duty 3's 86.5 unattributed pass breaks/frame on `framebuffer_dirty()` memcmp-ing the whole `SurfaceShape` including `clip_x/clip_y/clip_width/clip_height`, and proposed making it clip-invariant - ceiling +85% on that title | the surface's own dimensions are derived from the clip rectangle: `get_surface_dimensions` (surface.c:96) sets width/height from `clip_width`/`clip_height` in the non-swizzle path, and `populate_surface_binding_target` (surface.c:1457) then adds `clip_x`/`clip_y` on top, with the comment "since we determine surface dimensions based on the clipping rectangle, make sure to include the surface offset as well" | **not implemented.** Excluding clip from the dirty test would leave an undersized surface bound - a correctness bug, not a slower number, and the kind that shows as corruption. The 86.5 breaks are real and still the largest single GPU cost in the corpus, but the mechanism is not clip invariance. Next candidate is surface *reuse*: a binding large enough for a smaller clip could be kept instead of rebuilt, which needs the matcher rather than the dirty test |
| 90 | **Clears kept inside the render pass (`XEMU_CLEAR_INLINE`) + per-site pass-break counters** | a clear ends the pass, clears in a fresh one and ends that too, though `vkCmdClearAttachments` is an in-pass command; the workflow priced a boundary at 0.40 ms and predicted +6% Morrowind / +10% Tony Hawk | mechanism confirmed: Morrowind renderpasses **6.45 -> 2.11**, gpu_busy 14.53 -> 12.49 (-2.04 ms against 1.74 predicted from 4.34 passes x 0.40, so the coefficient holds), wait fell in lockstep 12.51 -> 10.88. But Tony Hawk shows the catch: **rpb_clear 2.80 -> 0.00 while rpb_surface 3.16 -> 4.89**, net renderpasses only -0.93. fps: Morrowind +5%, Tony Hawk -5% - **both within noise**; Tony Hawk stutters -12% | **kept opt-in, default off - no measured fps win.** The new per-site counters are the result worth having: **pass breaks are partially conserved**. Holding a pass open means the next surface change has to break one that would already have closed, so eliminating a clear break creates a surface break. Raw `renderpasses` hides this; only the attribution shows it. Confirms the 0.40 ms/pass price on hardware, and redirects the work to `rpb_surface` - the site behind Call of Duty 3's 86.5 unattributed breaks/frame |
| 89 | **PGO REFUTED (roadmap item 8)** | `helper_lookup_tb_ptr` is 23% of the saturated thread, so better block layout looked worth ~4.6% | matched `-O3 --enable-lto` both stages, 1301 profiles from 180 s of real gameplay, **0 mismatched functions on the hot path**, binary 135 MB -> 107 MB. Same source, only compilation differs: fps +3%, p50 +2%, p99 -1%, stutters -5% - **all within noise**; **1% low -13% REGRESSED** | **not shipped.** The profile demonstrably applied (0 hot-path mismatches, binary 21% smaller), so this is not a build failure - PGO simply does not help here. Row 17 already said why: the helper is frequency-bound, not miss-bound, and layout cannot reduce how often the guest takes an indirect jump. Five build attempts; the flag mismatch and the SIGKILL-vs-clean-exit training trap were real blockers but the answer underneath was still no |
| 88 | **Owner's three-game session: the bottleneck is per-game, and a shipped fix is visible in their own logs** | per-game limits were inferred from snapshots, and the hot-vertex fix had only synthetic evidence | Tony Hawk 3 **37.1 fps** (GPU 11.1, host-work 17.5 - neither bound, ceiling 57); Morrowind 21.5 fps (GPU 10.3, **host-work 46.6 - CPU-bound**, ceiling 21); Call of Duty 3 17.0 fps (**GPU busy 41.4** of a 63 ms frame, host-work 25.1 - **GPU-bound**, ceiling 24). `vertex_buffer_dirty` was **34-43 ms/frame** in their pre-fix Morrowind logs and is **0.18 ms** today | the hot-vertex-region fix is confirmed on the owner's hardware across months of real play, not just snapshots. No single next change helps all three: Morrowind wants CPU work (PGO, running), CoD3 wants GPU work (output is forced to 1920x1080 while the guest renders 640x480). Tony Hawk needs nothing |
| 87 | **Row 86 CORRECTED by the owner's longer sessions - the two games have opposite bottlenecks** | row 86 read CoD3's `flip_stall` as submission serialisation, from a 2.3-minute sample where GPU busy was 13.4 ms | CoD3 over 8.6 min (252 batches/frame): **GPU busy 41.4 ms** of a 63 ms frame, flip_stall 36.7, host-work only 25.1 - **GPU-bound**, ceiling ~24 fps. Morrowind over 5.1 min (385 batches/frame): GPU busy 10.2, **host-work 51.5**, waits 9.5 - **CPU-bound**, ceiling ~19 fps | **multi-slot submission is not the general answer.** CoD3's flip stall is the CPU waiting on a saturated GPU, which overlapping cannot remove; Morrowind's frame is dominated by host work, not by its 9.5 ms of waits. A short sample picked the wrong scene and inverted the diagnosis. Per-game direction instead: Morrowind -> CPU (PGO on helper_lookup_tb_ptr, 23% of the saturated thread); CoD3 -> GPU (output is forced to 1920x1080 while the guest renders 640x480, and row 30 measured resolution moving GPU cost on this board). Take a long session before diagnosing a game |
| 86 | **Owner's own Call of Duty 3 session reframes the dominant wait** | Morrowind's dominant wait is `stalled` 12.2 ms (GPU reports) with `flip_stall` at 0.93, so submission pacing looked report-specific | CoD3, 2.3 min of real play, 94.8 batches/frame: fps 25.2, p50 41.5, **`flip_stall` 9.65 ms of 11.9 ms total wait**, `stalled` absent. Overlap model: host-work 29.6 + wait 11.9, ceiling ~34 fps against 25.2 actual | **both games reduce to one cause.** `pgraph_vk_finish` opens with an unconditional `pgraph_vk_wait_for_submission`, so the deferred-flip path (default on, and confirmed active) only moves the wait to the next finish's entry rather than removing it - the conservation row 16 predicted. With one submission in flight, every finish drains the previous one. Different games pay it under different reasons: Morrowind through occlusion reports, CoD3 through flips. That makes multi-slot submission the highest-value remaining item, ahead of PGO (~4.6% of the saturated thread), because it targets ~10-12 ms of a 42-53 ms frame in both |
| 85 | **Hard-FPU helpers re-measured after a regression report (#2988 comment)** | a tester reported that turning hard FPU off doubled their frame rate; row 24 had claimed p99 -8%, 1% low +14% | `XEMU_HARD_FPU=0` vs `=31` on mwplay2, guest work comparable: fps +4%, p50 +3%, p99 +1%, stutters -3% - **all within noise**; 1% low -5% | **no regression on aarch64** - the report does not reproduce here, and their system takes the x86_64 branch, which #2981 leaves untouched (it splits `g_use_hard_fpu` into inline/helpers, both still driven by `g_config.perf.hard_fpu` there). Row 24 is not contradicted: its before arm ran at 21.3 fps / p50 45.3 against today's 17.8 / 66.6, a far lighter scene whose after arm reached the guest's 30 flips/s cap. The helpers pay where x87 is a meaningful share and the guest is near its ceiling, and wash out in a heavier scene. Claim is scene-dependent, not wrong |
| 84 | **Pricing the GPU report stall by eliding it (`XEMU_NO_REPORT_STALL=1`, UNSAFE) - METHOD REFUTED** | `stalled` is 12.2 ms of 12.9 ms total host wait; eliding it should price the multi-slot submission refactor the way `XEMU_WEAK_MO` priced the ordering fences | harness rejected the pair: guest progress 1000000 -> 10000 (10x) and **begin_ends 1041 -> 1.00**. The reported +204% fps / -97% stutters are 54 fps of a blank screen | **unattributable - the method does not transfer.** Eliding a fence leaves the guest running; eliding an occlusion result does not, because Morrowind blocks on the value and stops submitting work. Confirms the stall is a real data dependency, not gratuitous waiting - so the refactor (submit when the report is queued, so the GPU finishes before the guest drains its pushbuffer) has to be built to be measured. Switch left in, default off |
| 83 | **Gameplay CPU profile (perf, 45 s of Morrowind, 6779 samples)** | host-work 42.4 ms/frame vs GPU busy 12.0 ms - the frame budget said CPU-bound but not where | `mttcg_cpu_thread_fn` 66.2%; **`helper_lookup_tb_ptr` 15.2% self - hottest leaf in the profile**; `pfifo_thread` 19.5%; `pgraph_method` 17.4%; audio `voice_worker_thread` 6.9% + `mcpx_apu_frame_thread` 3.9% = **10.8% on audio**; `create_pipeline` 8.45% total but only 0.25% self (children are texture binding, not compilation) | reorders the roadmap by measured share: TCG dispatch first, audio second (newly quantified), per-batch draw path third. Confirms rows 17/77 - the helper is frequency-bound, and chaining is enabled (`CF_NO_GOTO_TB` set only by singlestep/one-insn/log), so its cost is the guest's indirect jumps, not a tunable |
| 82 | **Persistent pipeline + SPIR-V disk caches - NO GAMEPLAY GAIN** | caches absent upstream entirely; every launch recompiled | cold->warm on identical guest state: fps +3%, p50 -0%, stutters -4% - **all within noise**, p99 nominally +7%. Cause is not a broken cache: it writes (602 KB) and reloads (byte-identical warm, nothing recompiled), but only **27 shaders / 73 pipelines are created across 3089 frames**, and a 45 s gameplay profile contains **zero** glslang/vkCreateGraphicsPipelines/vkCreateShaderModule samples | kept as an efficiency/correctness fix, **not** a frame-rate fix. Real benefit is confined to startup (boot window 20.5 -> 43.7 fps, p99 1282 -> 309 ms, single noisy samples). Upstream PR to be written with no fps claim. Two traps found: the save path was unreachable (`ops.finalize` runs on renderer switch, not on quit - needs an exit handler), and a GLib shutdown assertion that looked like a regression is **pre-existing**, hidden for 130 runs because the harness always SIGKILLs |
| 81 | **Owner's full 9.5-minute session on today's build** (113 windows, 374 draw batches/frame) | their 3.4-minute session before the defaults shipped | **p50 106.0 -> 56.1 ms (-47%)**, p99 -38%, **1% low 5.61 -> 8.52 fps (+52%)**, fps +31%, GPU busy -69%, host waits 37.9 -> 9.5 ms, vertex stall 34.11 -> 0.18 ms | Real play, their scenes, not a lab scene. Morrowind now sits AT its CPU ceiling: host-work 46.6 ms vs an overlap model predicting ~21 fps, and it measures 21.5. Further gains require guest-CPU work, nothing else |
| 80 | **REAL-WORLD confirmation from the owner's own play** (Morrowind, their scenes, 3.5 min each) | their session at 04:01, before today's defaults | their live session at 13:38: **fps 16.4 -> 24.4 (+49%)**, p50 106.0 -> 64.0 ms (-40%), 1% low 5.6 -> 8.1 (+45%), GPU busy 32.9 -> 9.0 ms (-73%), **vertex stall 34.11 -> 0.48 ms (-99%)**, host waits 37.9 -> 8.9 ms | Not a controlled A/B - different scenes - so the fps figure carries the usual caveat. The mechanism metric does not: the stall that ate a third of every frame in real play is gone. New cost `need_buffer_space` 1.85 ms, the inline ring filling, exactly the predicted trade |
| 79 | **Upstream patch prepared for the vertex hazard** (branch `vtx-hazard`, not yet pushed) | our fix carries local scaffolding - deferred submission, hot-region redirect, perflog counters - none of which upstream has | rebuilt minimally on `upstream/master`: **12 insertions, 2 deletions, 4 files, zero added comments**. Upstream `pgraph_vk_finish` submits and waits on UINT64_MAX with no in-flight submission at all, so the second bitmap our version needs is unnecessary there | Needs a build and a gameplay A/B on hardware before opening. Blocked only on the owner finishing their play session |
| 78 | **Thread pinning and SCHED_FIFO REFUTED** | one thread pegged at 99% while 2.4 cores idle - migration or preemption looked like plausible costs | p50 none 60.2 / pinned 58.4 / FIFO 58.4 / both 58.6 ms; fps 19.5 / 20.6 / 20.4 / 20.0 - a 3% band, inside noise | Applied externally to the running thread, no rebuild. The saturated thread is not losing time to the scheduler; it is simply doing the work. Platform tuning (priority 7) has nothing left here |
| 77 | **8192-entry TB jump cache REFUTED** | `helper_lookup_tb_ptr` is 20.5% of the saturated thread, and 15 bits had regressed - 13 bits was never tried | two paired runs: p50 +0% / -4%, fps +2% / +7%, 1% low -3% / +5% - all noise, no consistent direction | Reverted to 12 bits. Capacity is not the constraint at any size tested; the cost is the call itself. Recorded in the header so it is not retried a third time |
| 76 | **Per-thread CPU reframes the whole roadmap** | assumed the renderer/submission model was the remaining bottleneck | **one thread saturated at 99.2%**, renderer thread 30.3%, total 154.6% of 400% - the box is **61% idle**. Profiling that thread alone: `helper_lookup_tb_ptr` **20.5%**, x87 hard helpers ~10%, TLB ~5.5%, SSE ~2.8% | The constraint is the emulated CPU on ONE thread, not the GPU, not the renderer, not throughput. Multi-slot submission (roadmap item 4) would optimise a thread with 70% headroom - it is NOT the next win. Whole-process profiles understated the critical path by a third |
| 75 | **Apparent THPS3 regression was noise** | one pair read p99 +10%, 1% low -8%, stutters +53% on the title the owner says plays great | isolating the deferred flip gave p50 -13% / p99 -7% / 1% low +13% (pair 1) and all-noise (pair 2) - never negative. Control arms across three near-identical runs: p99 **44.96 / 49.81 / 47.32**, 1% low **19.55 / 17.30 / 18.66** | THPS3's own run-to-run spread is ~+-10% on p99, which explains the reading. No reproducible regression. Worth knowing: this title needs paired repeats, single pairs cannot resolve 10% here |
| 74 | **THPS3 gameplay benchmark added** (`thps` snapshot, The Foundry) | the control title had no deterministic scene | 203 draw batches/frame, 39.6 fps, p50 30.5 ms, **no vertex stall at all** - matching its own gameplay log | Third gameplay snapshot. Its value is as a regression guard: with no vertex stall to remove, today's changes can only cost it |
| 73 | **10-minute soak on today's defaults** (118 windows, Morrowind gameplay) | every benchmark so far ~2 minutes; a player is not | **zero** Vulkan errors, validation messages or asserts; no crash; CPU held 2800 MHz, 56.5 -> 57.8 C so no throttling. `queue_submit` 1.9 -> 2.0 and `mirror_sync` 0.3 -> 0.0 across the whole run - both fixes durable, no leak | p50 stepped 58.7 -> 66.7 ms at ~2.5 min and stayed flat. NOT drift: guest draw batches rose 975 -> 1171 (+20%) at the same instant, a scene change. Frame time +13.6% for +20% geometry is consistent |
| 72 | **4K tier activated for the whole N64 library** | only Ocarina ran 4K; Majora, MarioKart, RE2, Mario 64, Banjo x2 were still on their HD tier | switched by hardlinking `.hts.4k` onto the `.hts` name - zero disk cost, HD kept reachable via `.hts.hd`, reversible. Verified: Majora **22 GB** (RSS 354 MB), MarioKart **12 GB** (275 MB), Mario 64 **5.2 GB** (297 MB) - all ACCEPTED, all rendering in-game | Pack size is irrelevant to memory because prebuilt `.hts` streams from disk. 22 GB costs 354 MB resident |
| 71 | **Pack acceptance can be proved without eyeballing** | judged by comparing screenshots | a rejected pack is CLEARED by GLideN64, so a byte-identical pack size after a real run proves acceptance. Caveat: only if the run actually rendered - an X11 run that failed DRI2 auth left the pack untouched too, at 162 MB RSS vs 354 MB on the real KMS path | Verification must confirm the emulator rendered before trusting an unchanged pack. RSS and a real captured frame are the guards |
| 70 | **APU voice-worker oversubscription REFUTED** | 4 voice workers (one per logical core) on a 4-core board alongside vCPU, renderer, UI, APU-frame and vblank threads - looked like obvious contention | workers 4 / 2 / 1: p50 56.9 / 58.6 / 58.5 ms, fps 21.6 / 21.3 / 21.2, 1% low 12.2 / 12.0 / 12.5 - all within noise, p50 marginally WORSE with fewer | The threads are mostly blocked, not competing. Config-only test, no rebuild. `audio.vp.num_workers` left at auto |
| 69 | **CPU profile re-taken after the three defaults shipped** | distribution assumed to have shifted once `mirror_sync` fell 99% | essentially unchanged: `helper_lookup_tb_ptr` 13.0%, `pgraph_vk_mark_textures_possibly_dirty` 3.0%, x87 `__hard` ~4.9%, TLB ~2.6%, audio ~3.0%, SSE helpers ~1.6% | Renderer bookkeeping is the largest item under our control and is worth only ~1.4 ms of a 57 ms frame. No CPU-side lever above ~2-3% frame time remains |
| 68 | **END-TO-END, corrected**: today's defaults vs everything off | all three defaults disabled | **Morrowind p50 114.9 -> 56.9 ms (-51%)**, p99 -40%, 1% low +23%, GPU busy -74%. **GTA fps 24.1 -> 27.4 (+14%, now IMPROVED not noise), p50 -20%, p99 -27%, 1% low +32%, stutters -50%** | GTA's fps verdict flipped from "within noise" to IMPROVED once the paused windows stopped inflating variance |
| 67 | **Snapshot restore was contaminating every measurement** | `avg`/`worst`/per-window sd taken at face value | a `loadvm` pause is recorded as one 16.4-SECOND frame; excluding windows with any frame over 1000 ms drops GTA's per-window sd from **6.9 to 1.8** and its p99 from 80.2 to 57.4 ms | The harness was poisoning its own numbers, and the inflated variance was suppressing real verdicts as "noise". p50 was always robust; avg and worst never were |
| 66 | **END-TO-END: today's defaults vs everything off**, both gameplay snapshots | all three defaults disabled | **Morrowind p50 105.2 -> 55.0 ms (-48%)**, p99 -43%, 1% low +29%, GPU busy -74%. **GTA p50 39.2 -> 31.8 ms (-19%)**, p99 -20%, 1% low +32%, stutters -49% | Combined effect of the hot-region redirect, precise vertex tracking and the deferred flip. Morrowind's stutters/min reads +81%, but p99 and 1% low both improved sharply - the 50 ms threshold is simply badly placed once p50 halves, and the metric's noise floor is +-27% (row 40) |
| 65 | **Deferred flip made default after repeats** | one pair suggested p99 +6% on Morrowind | three Morrowind pairs: p99 **+6 / -9 / -4%** (mean ~-2%, i.e. noise); pair r2 also p50 -8%, 1% low +16%. GTA pair: p50 -10%, p99 -7% | SHIPPED. The single-run regression that blocked it did not reproduce. Repeating before deciding was worth two runs |
| 64 | **Deferred flip wait re-tested after the redirect** (`XEMU_ASYNC_FLIP`) | flip_stall was GTA's largest remaining wait at 15.0 ms | GTA: **p50 -10%, p99 -7%**, flip_stall -56%, frame check MATCH. Morrowind: flip_stall -87% but p50 +2%, **p99 +6%**, fps -1% | NOT defaulted - genuinely mixed across two games, one run each, and a 6% p99 delta sits near the noise floor. Stays opt-in. Worth repeats before deciding |
| 63 | **Framebuffer lifetime fixed for the deferred path** | `destroy_framebuffers` destroyed every framebuffer on reclaim, including ones the still-recording command buffer had begun render passes against - latent, reachable only with `XEMU_ASYNC_FLIP` | reclaim now destroys only the submitted ones and shifts the rest down; deferred-flip run returned frame check MATCH | Correctness fix for a hazard found by reading, not by a crash. Absence of corruption in one run is not proof it was ever hit, but the lifetime is now correct by construction |
| 62 | **Redirect validated on a SECOND real game** (GTA San Andreas gameplay snapshot, 462 draw batches/frame) | vertex stall 15.59 ms/frame, 8.97 stalls/frame | **stutters/min 368 -> 51 (-86%)**, 1% low +35%, p99 -22%, vertex stall -95%, GPU busy -18%; p50 and fps within noise | Win GENERALISES but changes shape: Morrowind gained throughput (p50 -44%), GTA gains consistency. Its stall was 15.6 ms of a 37 ms frame, so removing it takes out the hitching rather than raising the average |
| 61 | **GTA gameplay reachable by synthetic input** | Halo blocked - its profile save is unloadable, loops on an ATTENTION dialog that neither A nor START clears (pre-existing, not caused by our changes) | GTA drives title -> START -> loading -> gameplay in four held keys; snapshot `gtaplay` | Second deterministic gameplay benchmark. Halo remains unbenchmarked; the owner's own next session is the cheaper route there |
| 60 | **`stalled` wait is not cheaply narrowable** | 10.4 ms/frame, the largest remaining wait | `process_pending_reports_internal` asserts no command buffer is open, so reports cannot resolve until the open buffer is closed - the full finish is already minimal for a single submission slot | Only multiple in-flight submissions (roadmap item 4) change this. Investigated and rejected rather than attempted |
| 59 | **Audio watermark theory REFUTED by measurement** | leading cited explanation for the owner's audio lag: `queued_bytes_high = 3x` an unbounded device buffer, with a +-1 us/frame servo that cannot recover | measured on this board: device buffer 1024 frames, **low 21 ms / high 64 ms** - an ordinary latency, not the hundreds of ms the theory required | Fix written then DELETED unshipped. The remaining candidate is voice cursors advancing at host rate while the guest runs below realtime, which the 44% frame-time win directly reduces |
| 58 | **Redirect re-verified under the shipping configuration** | every earlier A/B ran without TSO, hard FPU and direct surface sampling | with all three on: **p50 106.3 -> 59.5 ms (-44%)**, p99 -39%, 1% low +27%, GPU busy -74% | Fourth independent confirmation (-40/-37/-41/-44%). The win is slightly larger with the real settings, not smaller |
| 57 | **Benchmark harness was not measuring the shipped configuration** | `bench.sh` set only perflog/fps/backend | the launcher also exports `XEMU_TSO=1`, `XEMU_HARD_FPU=31`, `XEMU_SURF_TEX_SAMPLE=1`; none were present in any benchmark before this | A/B verdicts stayed valid (both arms identical) but absolute numbers described a configuration nobody plays. bench.sh now mirrors the launcher, overridable per variable |
| 56 | **CPU profile of real gameplay, shipping configuration** | frame time no longer bounded by GPU or renderer stalls | `helper_lookup_tb_ptr` **13.8%**, `pgraph_vk_mark_textures_possibly_dirty` 3.5%, x87 `__hard` helpers ~5.3% (was ~12% as `__soft`), TLB ~3.2%, audio ~3.2%, SSE helpers ~1.7% | Profile is flat below the top entry. TB lookup is call-frequency bound on ret-heavy guest code, and row 12's measurement already showed growing the jump cache regresses on an A76. Confirms the hard-FPU work: softfloat share more than halved |
| 55 | **Hot-region redirect made default** (`XEMU_VTX_HOT_REMAP=0` to opt out) | mirrored baseline | default build vs baseline: **p50 -41%, p99 -39%, 1% low +45%, GPU busy -76%**, vertex stall 59.71 -> 0.24 ms | SHIPPED. Reproduced three times (-40/-37/-41% p50). Inert where it is not needed, so no risk to titles without the stall |
| 54 | **Redirect is inert without conflicts** (Halo menu scene, 0.02 conflict pages) | - | every metric identical between arms, frame check MATCH | Safety property that justified defaulting it: the code only engages where a region is actually being recycled. Note the harness correctly REFUSED this pair as a benchmark - 37.8 draw batches/frame tripped the gameplay guard from row 45 |
| 53 | **Hot vertex regions fetched per draw** (`XEMU_VTX_HOT_REMAP`) - Morrowind gameplay snapshot, one binary, env-gated | p50 114.0 ms, GPU busy 52.65, 102 submissions + 107 render passes/frame | **p50 68.9 ms (-40%)**, p99 -39%, 1% low +38%, **GPU busy 12.92 (-75%)**, submissions **1.87 (-98%)**, render passes **7.58 (-93%)**, vertex stall 59.71 -> **0.15 ms**, frame check **MATCH** | Largest win of the project. Each drain forced a submission AND a render-pass break; on a tiler every break is a tile store+reload, which is why GPU time fell with it. Morrowind now profiles like THPS3. Costs: `stalled` 0.55 -> 10.72 ms, flip_stall 0.67 -> 4.39, stutters/min +59% (partly an artifact of 39% more frames against a fixed 50 ms threshold) |
| 52 | **Vertex conflicts are concentrated, not diffuse** | assumed spread across the 64 MiB mirror, implying the submission model had to be rebuilt | **5 of 64 MiB buckets see any conflict; bucket 47 alone holds 66-67%** across every window | Changed the fix entirely: a recycled dynamic vertex buffer can be routed around for a few lines, where multiple submission slots would have been a large risky refactor. Measuring locality before designing was worth more than the design work |
| 51 | **Precise vertex tracking made default** (`XEMU_VTX_PRECISE=0` to opt out) | 3.36 missed hazards/frame | `vtx_missed_hazards` **3.36 -> 0.00**; fps 15.37->15.17, p50 107.8->109.5, vertex stall 55.67->56.40 - every timing metric within noise | SHIPPED - a real correctness hole closed at no measurable cost. Same guest work both arms, deterministic snapshot |
| 50 | **Vertex guard hole CONFIRMED in real gameplay** | derived from source, unreproduced (row 43) | instrumented count: **3.27-3.36 per frame** of rewrites landing on pages a recorded draw still reads | Live bug in default xemu behaviour, not a theoretical one. Candidate explanation for the submenu text corruption reported by the owner |
| 49 | **Deterministic real-gameplay benchmark achieved** (`mwplay2` snapshot) | attract demo, 4.7 draw batches/frame, no vertex stall | Morrowind character creation in-world: **918.3 batches/frame**, vertex stall **55.5 ms of a 109 ms frame (51%)**, 93 stalls/frame | Heavier than any of the owner's own sessions (283-585). Required three fixes: hold keys across the input poll, unbind physical pads so USB topology is stable, and restore snapshots over the monitor because xemu hot-plugs controllers after machine init so `-loadvm` cannot work |
| 48 | **Synthetic input needs holding, not tapping** | `xdotool key` used to drive the guest | xemu never consumes key *events*: `xemu_input_update_sdl_kbd_controller_state` samples `SDL_GetKeyboardState()` when the UI loop polls, and that loop is now vblank-paced, so a sub-millisecond tap falls between two 16 ms polls | Keys must be held (`keydown`, sleep, `keyup`). Also: no `--window` on xdotool - that path is XSendEvent and SDL discards synthetic events. Ironically row 37's vblank pacing is what made taps unreliable |
| 47 | **Morrowind's attract mode is not rendering the world** | attract fly-through assumed to be real geometry, just camera-driven | 4.7 draw batches per presented frame against 478 in real play - two orders of magnitude too low for Vivec. It is pre-rendered stills with cross-fades, which is also the source of the 'ghosting' misread in row 41 | Explains three misleading results in one go. Halo CE's attract mode by contrast runs a real AI gameplay demo, so it is a gameplay-grade benchmark needing no controller input |
| 46 | **Mechanism of the vertex stall, per frame** | stall known but not explained | `fin_vertex_dirty` / `queue_submit` per frame: THPS3 0.01/2.0, Halo 0.24/1.7, GTA 7.2/8.2, **Morrowind 53.4/55.2**. `mirror_sync` 7.5 / 1.5 / 22.1 / **106.8** | Morrowind drains the pipeline 53x per frame; ~half its 107 vertex uploads collide. Submissions track drains almost 1:1, so each one also breaks the render pass - a tile store+reload on V3D. Halo is the opposite shape: few drains, each long because its GPU is busy 49.9 ms |
| 44 | **Cross-game analysis of the owner's real gameplay logs** | vertex stall assumed to be one game's problem | THPS3 (the one game that plays well) has `vertex_buffer_dirty` **0.00** ms/frame; Halo 13.17, GTA SA 18.74, Morrowind 31.84 - fps ranks inversely | The vertex stall is THE differentiator between games that run well and badly on this board. Fixing it is worth real memory, and it now has evidence across four titles |
| 45 | **Gameplay threshold calibrated** | benchmark scene judged by eye | real play measures 283 (THPS3), 308 (Halo), 478 (Morrowind), 585 (GTA SA) draw batches per presented frame; Morrowind's attract demo measures **4.7** and shows no vertex stall at all | Report now rejects any A/B below 50 batches/frame. The attract demo is ~100x lighter than play and misses the dominant cost entirely |
| 43 | **Hole found in the default vertex guard** (`vertex.c`, source reasoning - not yet reproduced) | guard tests pages *uploaded* since the last reclaim | a page uploaded in an earlier command buffer, read by a draw recorded in the current one without re-upload, carries no bit; a guest rewrite then lands on data an unexecuted draw still reads | OPEN - `uploaded` is not the same set as `read by a recorded draw`. Needs a reproducer before any fix; the in-place mirror means the only complete fix is versioning the buffer |
| 42 | **Precise vertex-rewrite tracking** (`XEMU_VTX_PRECISE`, off by default) | guard fires on any page *uploaded* | conflicting pages 10.76 -> 0.15/frame; stalls 0.12 -> 0.15/frame (+20%), p99 +43% | RECLASSIFIED as a CORRECTNESS fix, not a speed one - see row 43. It is slower because it tracks a strictly larger set, and that set is the one correctness actually requires |
| 41 | **Attract-mode cross-fade mistaken for corruption** | ghosted frame read as a rendering bug, change reverted | the *default* guard produced a ghosted frame too, on a path that gave crisp frames twice before - 3 captures of identical rendering, 2 crisp 1 ghosted | CALIBRATION - ghosting is time-dependent, not code-dependent. Morrowind's attract demo cross-fades between viewpoints; it is unfit as a visual-regression scene. Needs a gameplay snapshot |
| 40 | **Stutter metric noise floor measured** | assumed meaningful at ~10% | two *functionally identical* binaries (70928c507 vs its revert b55b91b3c) differed by **+27% stutters/min** on the same snapshot | CALIBRATION — treat stutters/min deltas below ~30% as noise; do not call them regressions |
| 39 | **Conditional surface-upload drain** (`surface.c`, guarded by `draw_time`/`sampled_time`) | unconditional `pgraph_vk_finish` per upload | waits down (aux_fence -33%, flip_stall -45%); frame looked **ghosted**, which was read as corruption | REVERTED, but the reason was WRONG - see row 41. Ghosting is the attract demo cross-fading, not the change. Verdict on this change is UNKNOWN, not "corrupts" |
| 38 | **Snapshot-resumed benchmarking** (`make-snapshot.sh` + `bench.sh -x`) | arms started from disc boot and drifted; a scene difference read as a perf difference | both arms resume one snapshot; guest-driven counters now agree within 15% | PASS - immediately exposed row 39's corruption, which the drifting benchmark had scored as MATCH 0.10 |
| 37 | **Vulkan present loop paced to vblank** (`ui/xemu.c`) | mailbox present never blocks, so the UI loop free-ran | p99 -6%, 1% low +14%, stutters -17%, waits down across the board | PASS - the GL path was vsync-paced all along; only the Vulkan arm was uncapped |
| 1 | Index-buffer clamp (bounded allocation) | 855 MiB reserved, 254 tex evictions | 580 MiB, 0 evictions | kept |
| 2 | 1080p output instead of 4K | — | +24% fps | kept (launcher forces 1080p) |
| 3 | V3D 1350 MHz | — | +9% fps, readback 18.14 -> 14.57 ms | kept |
| 4 | -O3 | — | +1.1% | kept |
| 5 | LTO | — | +1.0% | kept |
| 6 | Fence instead of vkQueueWaitIdle (aux submits) | — | no change | kept for correctness |
| 7 | Non-blocking presenter on the readback path | 16.6 fps | 6.3 fps | **reverted** — starved the upload |
| 8 | Vulkan swapchain presentation (`XEMU_DISPLAY_BACKEND=vulkan`) | 18.0-18.4 fps, readback 10.9-11.7 ms, 1% low 7.8 | 19.4-19.9 fps, readback 0, 1% low 9.2 | kept, opt-in (+7% fps, +18% 1% low) |
| 9 | Deferred flip-stall wait, first attempt (`XEMU_ASYNC_FLIP=1`) | 19.8 fps | 5.8 fps, assert, guest starved 1000x | **broken** — reclaim ran after next-draw allocation; fix at HEAD, untested |
| 36 | SSE hardfloat fast path (`XEMU_SSE_HARDFLOAT`), Morrowind (uncapped) | 43.3 fps, p50 23.3, 97 stutters/min | 43.8 fps, p50 23.1 — all within noise; **stutters +6% REGRESSED** | **not shipped.** The gate diagnosis was correct (softfloat was running) but the win does not materialise: SSE FP was ~6% of a thread that is no longer the sole limiter at this frame rate. Kept opt-in, default off |
| 35 | **Benchmark saturation discovered** | Halo 2 assumed CPU-limited | **Halo 2 runs at 30.0 flips/s on both sides of any A/B — it is at its native cap** | the primary benchmark can no longer measure CPU-side work. Guest-rate ceiling reached for this title; further CPU A/Bs must use an uncapped title (Morrowind, 56 of ~60) |
| 33 | Vertex-drain sizing (instrumentation, no fix yet) | 5.18-5.38 ms/frame, occurrence count unknown | **0.88 drains/frame at ~6.1 ms each** (not many small ones); 42 mirror syncs/frame, only ~2% conflict | one expensive mid-frame full drain per frame — real, but see row 34 |
| 34 | **Pacer re-measured with the full shipped stack** | assumed possibly shifted after TSO + hard-FPU | `CPU_0/TCG` **80.6%** (was 87.5%), renderer 31.7% (was 35.0%) | **the vCPU thread is still the pacer.** M1 and per-draw work are renderer-side, so expect fps-neutral like rows 16/27; their value is consistency only. Do not build M1 for throughput |
| 32 | Fallback path verified on hardware (`renderer = OPENGL`, V3D) | claimed working, unverified | probe fails -> GL declines -> **Vulkan renderer initializes and runs the guest**; ran to completion | confirmed. **Also found: two GL ceilings, not one** — the GUI needs GLSL 1.50 (`gl-helpers.cc`) and V3D caps at 1.40, so xemu aborts in HUD init before renderer selection unless the GL HUD is bypassed. Reported to #2979 |
| 31 | L3 soaks + **ship** | — | GTA SA locked 60.0 flips/s, **Morrowind 56 (was 27-55 without it)**, 0 errors both | PASS — `XEMU_SURF_TEX_SAMPLE=1` joins the launcher; full stack is now vulkan + TSO + hard-FPU + direct sampling |
| 30 | **Direct surface sampling (`XEMU_SURF_TEX_SAMPLE`)**, fixed binary, full stack both sides | surf_to_tex 6.18/frame, stutters/min 427, 1% low 11.5 | surf_to_tex **1.15** (residue = designed zeta/feedback/format fallbacks), **stutters/min 362 (-15%), 1% low 12.2 (+7%)**, gpu_busy -9%, parity + 0 errors | works as measured-for; soak on GTA+Morrowind gates the launcher. Debug trail: a null-labeler crash masqueraded as OOM and starvation first (rows in skill) |
| 29 | **System audio fixed** (all RetroPie emulators silent) | display returns 0-byte EDID -> no ELD -> vc4 refuses audio; Pi 5 vc4 PCM additionally accepts only IEC958_SUBFRAME_LE | conformant firmware EDID override (4K60+audio, `drm.edid_firmware=` + removal of the `video=...60D` force that skipped EDID probing) + `iec958` ALSA plugin chain in asound.conf | **audio infoframe live on the wire**; tone + wav play. Both fixes persist across boots |
| 28 | Stutter attribution (132 windows, 4 full-config runs, no new data) | hypothesis: compile stutter (M4) | r(pipeline_gen)=-0.15 — **compilation exonerated**; stuttery third = heavy scenes: begin_ends 85->1268/frame (r+0.92), nbs drains r+0.91, vbd settles r+0.85, surf_to_tex ~10/frame r+0.71, GPU at ~50% duty | M4 killed for this workload; remaining stutter levers are structural: per-draw host cost and direct surface-as-texture sampling (L3). Pool regrowth stays refuted (row 16) |
| 27 | Present-at-flip (`XEMU_PRESENT_ON_FLIP`), full launcher config both sides | 22.7 fps, p50 43.5 | 22.6 fps, p50 43.4 — everything within noise; parity, 0 errors | **not shipped** — correct but flat; in the measured scene the guest's own flip rate matched the presented rate, so no coalescing existed to recover. Stays as an opt-in for scene-specific investigation |
| 26 | **Config-parity audit from real play sessions** (user-reported lag) | launcher shipped `XEMU_HARD_FPU=1` (= bitmask group 1: loads only, arithmetic still softfloat) and never set `XEMU_DISPLAY_BACKEND=vulkan` (GL readback path, 5-14 ms/frame in play) | sessions: THPS2x 17.9 fps/554 stutters, Halo 2 18.2/518, Morrowind 11.1 fps 1%low 4.4 | **fixed** — launcher sets vulkan + mask 31; parser maps 1->31. Lesson: the bench env and the launcher env must be one thing |
| 25 | Hard-FPU soaks + **ship** | — | GTA SA at a locked 60.0 flips/s, Morrowind 27-55 (faster than its TSO-only run), 0 errors both | PASS — `XEMU_HARD_FPU=1` joins `XEMU_TSO=1` in the RetroPie launcher |
| 24 | **Hard-FPU helpers (`XEMU_HARD_FPU`, native-double x87), TSO both sides** | 21.3 fps, p50 45.3, guest ~28 flips/s | **p50 42.9 (-5%), p99 -8%, 1% low 12.0 (+14%), stutters -11%, guest at the native 30.0 flips/s cap**, parity + 0 errors | kept opt-in pending soak — **Halo 2 reaches its native frame rate**; v1 (unconditional LDAPR-era layout change) and v2 (inline-tier crash) both documented on the way |
| 23 | MXCSR trace (Halo 2, one LDMXCSR ever) | hypothesis: non-nearest rounding kills hardfloat | `mxcsr=0x1f80 rc=0 ftz=0 daz=0` — power-on default, nearest rounding | question closed: SSE hardfloat is healthy; remaining soft_f32 share is the near-denormal fallback. The x87 floatx80 path (~8.5%) is the only real FP target left |
| 22 | TSO soak 2: Morrowind (200 s) + **ship** | — | 0 errors, banner active, guest 29-52 flips/s, 40 fps presented | PASS — `XEMU_TSO=1` enabled in the RetroPie launcher |
| 21 | TSO soak 1: GTA San Andreas (`XEMU_TSO=1`, 200 s) | — | 0 errors, banner active, **guest at its native 60.0 flips/s, p50 16.7 ms**, 5 stutters total | PASS — second title clean; a 60 fps-class title reaches 60 under TSO |
| 20 | **TSO v2 (`XEMU_TSO=1`): x86-TSO via alignment-checked LDAPR/STLR** | 19.3 fps, p50 49.3, guest 22-23 flips/s | **21.5 fps, p50 44.4 (-10%), p99 -9%, 1% low +11%, stutters -17%, guest 27-28 flips/s**, parity + 0 errors | kept opt-in; recovers ~half to two-thirds of the unsafe fence-elision bound (row 18) with ordering preserved — candidate for default-on after soak |
| 19 | TSO v1 (unconditional LDAPR/STLR) | — | guest made zero progress: LDAPR/STLR fault on unaligned addresses and the A76 lacks FEAT_LSE2 to relax that; x86 code is routinely unaligned | **replaced by v2** — runtime alignment check, aligned -> ordered access, unaligned -> barrier + plain |
| 18 | Fence-elision measurement (`XEMU_WEAK_MO=1`, UNSAFE, same binary) | 18.3 fps, p50 52.0, 1% low 8.2 | **22.8 fps (+25%), p50 42.8 (-18%), p99 -21%, 1% low +53%, stutters -27%**, 0 errors | measurement only — prices the per-access dmb barriers; the shippable form is x86-TSO via LDAR/STLR (M6), now the top work item |
| 17 | TB jump cache 4k -> 32k entries, A/B vs row-15 base | p50 48.8, 1% low 9.1, stutters/min 509 | p50 52.0 (+7%), 1% low 8.2 (-11%), stutters/min 553 (+9%) — all REGRESSED | **reverted** — probes went from L2-hot (64 KB) to L2-thrashing (512 KB); helper_lookup_tb_ptr is frequency-bound, not miss-bound |
| 16 | Descriptor pool 1024 -> 4096 (N5), A/B vs row 14 | nbs_descriptors 0.185/frame, nbs wait 3.65 ms | nbs 0.000, nbs wait 0.03 — but p50 +6%, **p99 +19% REGRESSED**, flip/vertex waits +5 ms (drains moved, grew) | **reverted** — wait is conserved on a TCG-paced frame; fewer-but-larger submissions worsen the tail |
| 14 | Overlap-only settle (bitmap cleared at reclaim), A/B on bc80be5 | 19.7 fps, p50 48.8 | 20.3 fps (noise), **p50 46.1 (-6%, IMPROVED)**, stutters/min -8%, 0 errors | kept — the deferral's honest final form: consistency feature |
| 15 | N2 instrumentation first read (same run) | — | aux_fence 0.59 ms (M3 demoted); renderpasses 10.7/frame (M5 tile-churn hypothesis dead, ~1.3 ms); **nbs_descriptors is the only exhausting pool** (0.19/frame ~= 690 batches / 1024 sets); queries 0 | three roadmap gates resolved in one run |
| 13 | N1 safety triad (race-free deferral), A/B on bdda32f | 19.7 fps, flip wait 9.55 | 20.2 fps (within noise), flip wait 10.44 (!), stutters/min -7%, p99 +7%, 0 errors | the settle-before-every-mirror-write reclaims the deferral immediately: safe but mechanism gone; refine to overlap-only settle |
| 10 | Deferred flip-stall wait, repaired (reclaim at begin_pre_draw) | 19.6 fps, host wait 20.4 ms | 20.4 fps (within noise), host wait 10.2 ms (flip 9.70 -> 0.60), stutters/min -12%, 1% low -5%, 0 errors, guest work fair | kept opt-in — correct, but fps unchanged: **pgraph fence waits are not the frame-rate limiter** |

## Current frame budget (HEAD, Vulkan presentation, measured)

```
frame p50                47.1 ms   (19.9 fps)
GPU busy (timestamps)    17.71 ms  (GPU idle 62% of the frame)
host blocked on fences   20.21 ms
    flip_stall            9.59
    vertex_buffer_dirty   6.81
    need_buffer_space     3.62
    presenting            0.17
host work                ~26.9 ms
ideal CPU/GPU overlap    max(26.9, 17.7) ~= 27 ms  => ~37 fps ceiling
```

Host wait ~= GPU busy: the pipeline is serialised (record -> submit -> wait).

**Update (row 10):** removing half the host wait (20.4 -> 10.2 ms) left fps
within noise. The serialisation was real but not rate-limiting.

**Update (row 11, measured):** per-thread CPU during live gameplay
(vk + async flip, named via `-name xemu,debug-threads=on`):

```
  87.5%  CPU_0/TCG        <- guest CPU emulation: the wall
  35.0%  renderer (pfifo/pgraph, all Vulkan work)
   7.4%  mcpx.apu_thread
   1.4%  vblank-timer
 total  185% of 400% available (two cores idle)
```

The frame is paced by the TCG vCPU thread. GPU-side and renderer-side work
target components at 32-35% duty; the guest-CPU thread at 87.5% is where
frame time now lives.

**Update (row 12, measured):** perf on the TCG thread (30 s @ 997 Hz,
`-perfmap`, symbols >= 0.4%):

```
 10.4%  helper_lookup_tb_ptr        TB chaining / indirect-branch lookup
  2.3%  tlb_reset_dirty_range_all   dirty-page tracking
 ~8.5%  x87 softfloat               floatx80_mul/addsub, fsts/flds/fmul helpers
 ~5.4%  SSE FP helpers              mulss/mulps/addps/comiss + soft_f32_mul
 ~4.5%  exec machinery              cpu_exec_loop, tb_lookup_cmp, tlb_set_page
 ~69%   JIT-generated code + tail   guest code itself, incl. inline TSO fences
```

Reads on the roadmap gates: FP helper share is ~14% visible at the 0.4%
cutoff (x87 alone ~8.5%) — borderline for the L1 hard-FPU GO gate (>=15-20%),
with more FP certainly in the tail. `soft_f32_mul` appearing at all means the
float32 hardfloat fast path is falling through — worth checking whether the
guest sets FTZ/DAZ, which disables QEMU's hardfloat. `helper_lookup_tb_ptr`
at 10.4% is the single largest symbol and was on nobody's roadmap: indirect
branches missing the TB jump cache. The ~69% JIT tail is where M6's per-load
`dmb ishld` fences live, invisible to symbol profiling — only the elision A/B
can size them.

## Scene map: which snapshot exercises which path (2026-08-21)

Probed every bench snapshot with the counters rather than trusting its name.
Restores were confirmed live via QMP `query-status` plus a frame-hash check, so
these are running guests, not paused ones.

| snapshot | game | fps | queries/f | rpasses | gpu_busy | begin_ends |
|---|---|---|---|---|---|---|
| halo | Halo 1, outdoors | 29.68 | 0.6 | 4.7 | 3.5 ms | 60 |
| mwplay | Morrowind | 12.87 | 1.0 | 10.0 | 16.9 ms | 1172 |
| gtaplay | GTA:SA | 25.64 | 0.0 | 15.9 | 16.7 ms | 565 |
| thps | THPS3 | 46.23 | 0.0 | 7.7 | 10.4 ms | 288 |
| halo2, halo3 | - | - | - | - | - | aborts on restore |
| *owner's cryo bay* | Halo 1, Pillar of Autumn | **5.16** | **208.78** | **235.4** | **111.3 ms** | 573 |

Three things follow.

**No snapshot here can measure the occlusion-query path.** The most any of them
issues is one query per frame. The query work is a property of one *scene* - a
lit interior - not of the game. Any A/B of query handling run against these
snapshots measures nothing, however clean its numbers look.

**Most of these titles are CPU-bound, not GPU-bound.** Morrowind spends 16.9 ms
of a 77.7 ms frame on the GPU: 78% of that frame is guest emulation. GTA is
16.7 of 39.0. Vulkan work cannot reach them; they belong to roadmap item 5.
The cryo bay is the exception and the only strongly GPU-bound scene measured so
far, which is why it is also the only place a render-pass fix can pay.

**halo2 and halo3 abort on restore** with
`gp_ep.c:60: scatter_gather_rw: Assertion 'page_entry <= max_sge' failed` - the
MCPX APU scatter-gather table. Checked against all 21 owner session logs: zero
occurrences, so this is a snapshot-restore artifact and not something reachable
in play. Those two snapshots are unusable; the assertion is not a live bug.

## Open follow-ups

- **Multi-slot submission (roadmap item 4)** — now the highest-value remaining
  item, and the only one with evidence from two different games (row 86).

  Why the wait costs what it does: GPU busy is 13 ms against a 42-53 ms frame,
  so the GPU is idle most of the time. The wait is expensive because commands
  accumulate until a flip or a report forces a submit, and the result is needed
  almost immediately after - so the wait is roughly the GPU execution time,
  paid serially. Submitting earlier lets it overlap, which needs a second slot
  to keep recording into.

  `command_buffers[2]` is main + aux, not two slots. Everything
  `pgraph_vk_wait_for_submission` reclaims has to become per-slot before a
  second submission can be outstanding:

  - `command_buffer` and `aux_command_buffer` (so `command_buffers[2 * N]`)
  - `command_buffer_fence`, `aux_command_buffer_fence`, `command_buffer_semaphore`
  - `descriptor_set_index` — the pool range in use
  - staging offsets in BUFFER_*_STAGING (index, inline-vertex, uniform)
  - `uploaded_bitmap` and `referenced_inflight_bitmap` — the vertex-RAM guard
    reads these to decide whether an in-flight draw still references a page
  - the framebuffer list `destroy_framebuffers` walks
  - the timestamp query pool (currently 2 slots, one pair per submission)
  - `pgraph_vk_process_pending_reports_internal`, which currently runs from the
    wait and assumes the results it reads belong to the submission just retired

  Risk: this is the area that produced the framebuffer-lifetime bug, and the
  vertex-RAM guard is a correctness feature, not a performance one. Bisectable
  order: fences/command buffers first (no behaviour change with N=1), then
  descriptor and staging ranges, then the bitmaps, then raise N. Soak rather
  than A/B - a race here shows as corruption, not as a slower number.

- **Upstream `vk/present.c`** — the Vulkan swapchain presentation path is the
  one genuinely new file this work adds to the renderer (everything else is
  edits to existing files; upstream already ships a full Vulkan renderer).
  Blocked on completing the ImGui overlay for that path: it currently skips
  the GL HUD entirely, so a desktop user switching to it loses their menus.
  Needs `imgui_impl_vulkan` wired to the presenter's device/queue, then it is
  PR-ready as one new file plus edits to `ui/xemu.c` and `nv2a.h`.
- **GUI GLSL ceiling** — `ui/xui/gl-helpers.cc` uses `#version 150 core`;
  V3D caps at 1.40, so xemu aborts in HUD init on that hardware regardless of
  renderer fallback (ledger row 32). Lowering those shaders is what would make
  a GL-3.1 device genuinely supported upstream.
